#include <stdio.h>

int main()
{
    int num;

    printf("Enter any number: ");
    scanf("%d", &num);

    if(num & 1)
        printf("It is LSB = %d ", num);
    else
        printf("It is not LSB = %d ", num);

    return 0;
}
